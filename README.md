# Topology-change-detector
Detects dynamic changes in network topology by monitoring switch and link events. Updates the topology map in real time, displays changes, and logs events for analysis. Ensures accurate network visibility and supports efficient routing and management.
